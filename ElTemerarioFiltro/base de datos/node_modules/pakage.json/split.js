//splitting string

//strs is a array of four elements starting with zero

var data = 'java;oracle;c++;c';
var strs = data.split(';');
for(var index in strs)
{
	console.log(strs[index]);
}
    console.log(strs[2]);

//string function
var h='hello world';
console.log(h.length);
console.log(h.toUpperCase());
console.log(h.toLowerCase());
console.log(h.substr(2,6));
